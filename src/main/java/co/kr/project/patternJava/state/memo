state 패턴

각 상태에 따라 실행되는 항목이 자동으로 바뀌는 패턴
메소드가 실행될때 모드도 전환되도록 하는 것(다음에 사용될 클래스 주입)

최초 LIGHT모드로 설정된 어플이 생성 되었고 그때 다음에 사용될 클래스로
ModeState를 상속받는 DarkMode가 주입되어있다

그래서 다음 modeSwitch.onSwitch() 실행시 설정된 darkMode의 toggle이 실행된다