adapter 패턴

형식이 다른 둘 사이에 연결되어서 이 둘이 호환될 수 있도록 해주는 패턴
인터페이스가 서로 다른 객체들이 같은 형식 아래 작동할 수 있도록 하는 역할

strategy패턴 예제를 활용 하자면
검색할시 외부에서 만들어진 항목:영상검색 버튼을 추가했을시
상속받는 인터페이스가 다른 클래스를 사용해야 할때

SearchStrategy에 동영상 검색을 위한 어뎁터 클래스를 추가하여
MyProgram에서 setModelMovie() 실행시
외부 영상 검색클래스를 생성자로 받아 변환해주는 어뎁터 클래스를 전략에 주입하여
정상 실행하도록 한다
