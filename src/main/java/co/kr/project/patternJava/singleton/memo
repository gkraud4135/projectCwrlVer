싱글톤 패턴

휴대폰 어플에서 다크모드 설정시 모든 페이지에 해당
설정이 적용 되어야한다
이렇게 객체가 해당 프로세스에 한번만 생성되어야 하는경우
사용된다

Settings 설정 객체가 최초 getSettings로 생성 설정되어
각 페이지에서 getSettings 호출시 최초 static 생성된 객체를 리턴
사용한다

*멀티 쓰레드 환경등에서 오류가 발생할 소지가 있다